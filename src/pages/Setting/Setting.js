import { Link } from "react-router-dom";
import Header from "../../Layout/Header";

export default function Setting() {
    return (
      <div>
        <Header />
        <h1>Setting</h1>
      </div>
    );
  }